import 'package:flutter/material.dart';

class ClerkHomeScreen extends StatelessWidget {
  const ClerkHomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Clerk Home")),
    );
  }
}
